<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="author">
	<select id="bookList" parameterType="int" 
	resultType="dto.BookDTO">
	 select  b.bno,b.bname, b.bupdate,b.bgrade,b.bview,b.bthumb,b.binfo
	 from book b,book_author a
	 where a.auno=#{auno} AND a.bno=b.bno
	</select>
	
	<select id="authorin" parameterType="int" 
	resultType="dto.AuthorDTO">
	select DISTINCT a.auno,a.auname,a.auintro
	from book_author b ,author a
	where b.auno=#{auno} AND a.auno=b.auno
	</select>
	
	<insert id="insert" parameterType="map">
	insert into fav_author values(#{id},#{auno})
	</insert>
	
	<select id="search" parameterType="map" resultType="int">
		select count(*) from fav_author where id=#{id} AND auno=#{auno} 
	</select>
	

</mapper>