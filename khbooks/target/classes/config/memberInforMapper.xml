<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memInfor">
	<select id="list" parameterType="String" 
	        resultType="dto.UserDTO">
		select * from kh_user where id=#{id}
	</select>
	
	<update id="update" parameterType="dto.UserDTO">
		update kh_user set upass=#{upass}, uname=#{uname}, uemail=#{uemail}, uphone=#{uphone},
		where id=#{id}
	</update>
	
	<delete id="delete" parameterType="String" >
		delete from kh_user where id=#{id}
	</delete>
</mapper>


















 
 
 
 
 
 
