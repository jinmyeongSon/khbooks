<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="FavAuthor">
	<select id="list" parameterType="map" 
	        resultType="dto.AuthorDTO">
		<![CDATA[
	 select b.* from
     (select rownum as rm, a.*   from(
     select b.auno, b.auname, b.auintro
		from FAV_Author f , author b
		where f.id=#{id} AND f.auno=b.auno order by auno desc)a)b
     where b.rm>=#{pdto.startRow} and b.rm<=#{pdto.endRow}
	]]>
	</select>
	
	<select id="count" parameterType="string" resultType="int">
		select count(*) from FAV_Author where id=#{id}
	</select>
	
	<delete id="delete" parameterType="map">
		delete from Fav_author where auno=#{auno} AND id=#{id}
	</delete>
	
	<select id="search" parameterType="map" resultType="int">
		select count(*) from fav_author where id=#{id} AND auno=#{auno} 
	</select>
	
	<insert id="insert" parameterType="map">
		insert into fav_author values(#{id},#{auno})
	</insert>

</mapper>

